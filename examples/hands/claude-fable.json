{
  "id": "claude-fable",
  "vendor": "claude",
  "account": "claude-main",
  "model": "claude-fable-5-1",
  "cost": "high",
  "cmd": "claude -p --add-dir {taskdir} --permission-mode acceptEdits --allowedTools Bash --output-format json{model} {prompt}",
  "notes": "Deep work. {model} expands at run time to the model + effort flags, so `deck run --model/--effort` can override.",
  "effort": "high"
}
