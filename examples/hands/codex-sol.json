{
  "id": "codex-sol",
  "vendor": "codex",
  "account": "codex-team",
  "model": "gpt-5.6-sol",
  "cost": "mid",
  "cmd": "codex exec --sandbox workspace-write --add-dir {taskdir} --skip-git-repo-check --json{model} {prompt}",
  "notes": "--json emits turn. {model} expands at run time to the model + effort flags, so `deck run --model/--effort` can override.",
  "effort": "medium"
}
